\documentclass[11pt]{article}

\usepackage{../../ee16}
\usepackage{../../markup}

\renewcommand{\sol}[1]{} % no solutions
\renewcommand{\ans}[1]{} % no solutions
\renewcommand{\meta}[1]{} % no meta

\input{body}
